/**
 * Aparte Button Primitive
 *
 * The CLASS is the primitive — `.aparte-control` and its modifiers reach any `<button>`,
 * because core is light DOM. This element is the convenience over it.
 */

export { AparteButton, type AparteButtonClickEventDetail } from './aparte-button.js';
