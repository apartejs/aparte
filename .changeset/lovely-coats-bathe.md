---
---

Docs site only: Astro 5 → 7, Starlight 0.37 → 0.41. No published package changes, so this
changeset is deliberately empty — `@aparte-workspace/docs` is private but versioned, which
is why it needs an entry at all.
