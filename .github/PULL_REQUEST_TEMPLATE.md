## What

<!-- One sentence, for the caller: what changes from the outside. -->

## Why

## Checklist

- [ ] `pnpm test` green (and `pnpm gate` for a finished lot)
- [ ] One concern per commit, conventional message
- [ ] Changeset added if a package's public API or shipped CSS changed
      (first line = the change, for the caller — see CONTRIBUTING → "How a changeset reads")
- [ ] Docs page updated if a public API is new or renamed
